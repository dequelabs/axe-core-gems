version: 2

defaults: &defaults
  docker:
    - image: circleci/ruby:2.4.3-node-browsers
  working_directory: ~/axe-core-gems

jobs:
  test: 
    <<: *defaults
    steps:
      - checkout
      - run: rake test

workflows:
  version: 2
  build:
    jobs:
      - test